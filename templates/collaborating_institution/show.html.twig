{% extends 'base.html.twig' %}

{% block title %}CollaboratingInstitution{% endblock %}

{% block body %}
    <h1>CollaboratingInstitution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collaborating_institution.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collaborating_institution.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ collaborating_institution.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collaborating_institution_index') }}">back to list</a>

    <a href="{{ path('collaborating_institution_edit', {'id': collaborating_institution.id}) }}">edit</a>

    {{ include('collaborating_institution/_delete_form.html.twig') }}
{% endblock %}
