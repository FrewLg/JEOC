{% extends 'base.html.twig' %}

{% block title %}InstitutionalReviewersBoard{% endblock %}

{% block body %}
    <h1>InstitutionalReviewersBoard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ institutional_reviewers_board.id }}</td>
            </tr>
            <tr>
                <th>Specialization</th>
                <td>{{ institutional_reviewers_board.specialization }}</td>
            </tr>
            <tr>
                <th>Affiliation</th>
                <td>{{ institutional_reviewers_board.affiliation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('institutional_reviewers_board_index') }}">back to list</a>

    <a href="{{ path('institutional_reviewers_board_edit', {'id': institutional_reviewers_board.id}) }}">edit</a>

    {{ include('institutional_reviewers_board/_delete_form.html.twig') }}
{% endblock %}
