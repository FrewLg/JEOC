<form method="post" action="{{ path('institutional_reviewers_board_delete', {'id': institutional_reviewers_board.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ institutional_reviewers_board.id) }}">
    <button class="btn">Delete</button>
</form>
