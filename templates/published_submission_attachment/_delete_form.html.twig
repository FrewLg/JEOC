<form method="post" action="{{ path('published_submission_attachment_delete', {'id': published_submission_attachment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ published_submission_attachment.id) }}">
    <button class="btn">Delete</button>
</form>
