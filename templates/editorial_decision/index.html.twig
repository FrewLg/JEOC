{% extends 'base.html.twig' %}

{% block title %}EditorialDecision index{% endblock %}

{% block body %}
    <h1>EditorialDecision index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for editorial_decision in editorial_decisions %}
            <tr>
                <td>{{ editorial_decision.id }}</td>
                <td>{{ editorial_decision.name }}</td>
                <td>{{ editorial_decision.description }}</td>
                <td>
                    <a href="{{ path('editorial_decision_show', {'id': editorial_decision.id}) }}">show</a>
                    <a href="{{ path('editorial_decision_edit', {'id': editorial_decision.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('editorial_decision_new') }}">Create new</a>
{% endblock %}
