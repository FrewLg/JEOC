{% extends 'base.html.twig' %}

{% block title %}EditorialDecision{% endblock %}

{% block body %}
    <h1>EditorialDecision</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editorial_decision.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ editorial_decision.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ editorial_decision.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('editorial_decision_index') }}">back to list</a>

    <a href="{{ path('editorial_decision_edit', {'id': editorial_decision.id}) }}">edit</a>

    {{ include('editorial_decision/_delete_form.html.twig') }}
{% endblock %}
