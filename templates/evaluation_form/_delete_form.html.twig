<form method="post" action="{{ path('evaluation_form_delete', {'id': evaluation_form.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_form.id) }}">
    <button class="btn">Delete</button>
</form>
