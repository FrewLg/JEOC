{% extends 'base.html.twig' %}

{% block title %}EvaluationForm{% endblock %}

{% block body %}
    <h1>EvaluationForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_form.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaluation_form.name }}</td>
            </tr>
            <tr>
                <th>Percent</th>
                <td>{{ evaluation_form.percent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_form_index') }}">back to list</a>

    <a href="{{ path('evaluation_form_edit', {'id': evaluation_form.id}) }}">edit</a>

    {{ include('evaluation_form/_delete_form.html.twig') }}
{% endblock %}
