{% extends 'base.html.twig' %}

{% block title %}Guidelines{% endblock %}

{% block body %}
    <h1>Guidelines</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guideline.id }}</td>
            </tr>
            <tr>
                <th>Guideline</th>
                <td>{{ guideline.guideline }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ guideline.createdAt ? guideline.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ guideline.updatedAt ? guideline.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('guidelines_index') }}">back to list</a>

    <a href="{{ path('guidelines_edit', {'id': guideline.id}) }}">edit</a>

    {{ include('guidelines/_delete_form.html.twig') }}
{% endblock %}
