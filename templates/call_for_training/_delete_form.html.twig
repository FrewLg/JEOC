<form method="post" action="{{ path('call_for_training_delete', {'id': call_for_training.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ call_for_training.id) }}">
    <button class="btn">Delete</button>
</form>
