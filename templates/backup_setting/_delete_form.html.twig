<form method="post" action="{{ path('backup_setting_delete', {'id': backup_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ backup_setting.id) }}">
    <button class="btn">Delete</button>
</form>
