{% extends 'base.html.twig' %}

{% block title %}Suffixe{% endblock %}

{% block body %}
    <h1>Suffixe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suffixe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ suffixe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ suffixe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suffixe_index') }}">back to list</a>

    <a href="{{ path('suffixe_edit', {'id': suffixe.id}) }}">edit</a>

    {{ include('suffixe/_delete_form.html.twig') }}
{% endblock %}
