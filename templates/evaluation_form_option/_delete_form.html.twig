<form method="post" action="{{ path('evaluation_form_option_delete', {'id': evaluation_form_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_form_option.id) }}">
    <button class="btn">Delete</button>
</form>
