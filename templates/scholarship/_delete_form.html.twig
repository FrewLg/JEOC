<form method="post" action="{{ path('scholarship_delete', {'id': scholarship.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scholarship.id) }}">
    <button class="btn">Delete</button>
</form>
