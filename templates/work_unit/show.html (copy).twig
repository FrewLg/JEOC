{% extends 'base.html.twig' %}

{% block title %}WorkUnit{% endblock %}

{% block body %}
    <h1>WorkUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_unit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work_unit.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work_unit.description }}</td>
            </tr>
            <tr>
                <th>Principal_contact</th>
                <td>{{ work_unit.principalContact }}</td>
            </tr>
            <tr>
                <th>Identification_code</th>
                <td>{{ work_unit.identificationCode }}</td>
            </tr>
            <tr>
                <th>Mission</th>
                <td>{{ work_unit.mission }}</td>
            </tr>
            <tr>
                <th>Objective</th>
                <td>{{ work_unit.objective }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_unit_index') }}">back to list</a>

    <a href="{{ path('work_unit_edit', {'id': work_unit.id}) }}">edit</a>

    {{ include('work_unit/_delete_form.html.twig') }}
{% endblock %}
