{% extends 'base.html.twig' %}

{% block title %}AcademicRank{% endblock %}

{% block body %}
    <h1>AcademicRank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academic_rank.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academic_rank.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ academic_rank.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('academic_rank_index') }}">back to list</a>

    <a href="{{ path('academic_rank_edit', {'id': academic_rank.id}) }}">edit</a>

    {{ include('academic_rank/_delete_form.html.twig') }}
{% endblock %}
