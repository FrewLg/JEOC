<form method="post" action="{{ path('academic_rank_delete', {'id': academic_rank.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ academic_rank.id) }}">
    <button class="btn">Delete</button>
</form>
