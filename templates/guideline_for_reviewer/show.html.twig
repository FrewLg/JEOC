{% extends 'base.html.twig' %}

{% block title %}GuidelineForReviewer{% endblock %}

{% block body %}
    <h1>GuidelineForReviewer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guideline_for_reviewer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ guideline_for_reviewer.name }}</td>
            </tr>
            <tr>
                <th>The_guidelline</th>
                <td>{{ guideline_for_reviewer.theGuidelline }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ guideline_for_reviewer.createdAt ? guideline_for_reviewer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('guideline_for_reviewer_index') }}">back to list</a>

    <a href="{{ path('guideline_for_reviewer_edit', {'id': guideline_for_reviewer.id}) }}">edit</a>

    {{ include('guideline_for_reviewer/_delete_form.html.twig') }}
{% endblock %}
