<form method="post" action="{{ path('guideline_for_reviewer_delete', {'id': guideline_for_reviewer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guideline_for_reviewer.id) }}">
    <button class="btn">Delete</button>
</form>
