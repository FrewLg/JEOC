{% extends 'base.html.twig' %}

{% block title %}ThematicArea{% endblock %}

{% block body %}
    <h1>ThematicArea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thematic_area.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ thematic_area.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ thematic_area.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thematic_area_index') }}">back to list</a>

    <a href="{{ path('thematic_area_edit', {'id': thematic_area.id}) }}">edit</a>

    {{ include('thematic_area/_delete_form.html.twig') }}
{% endblock %}
