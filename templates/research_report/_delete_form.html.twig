<form method="post" action="{{ path('research_report_delete', {'id': research_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ research_report.id) }}">
    <button class="btn">Delete</button>
</form>
