<form method="post" action="{{ path('educational_level_delete', {'id': educational_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ educational_level.id) }}">
    <button class="btn">Delete</button>
</form>
