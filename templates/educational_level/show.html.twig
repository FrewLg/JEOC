{% extends 'base.html.twig' %}

{% block title %}EducationalLevel{% endblock %}

{% block body %}
    <h1>EducationalLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ educational_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ educational_level.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ educational_level.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('educational_level_index') }}">back to list</a>

    <a href="{{ path('educational_level_edit', {'id': educational_level.id}) }}">edit</a>

    {{ include('educational_level/_delete_form.html.twig') }}
{% endblock %}
