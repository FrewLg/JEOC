<form method="post" action="{{ path('user_feedback_delete', {'id': user_feedback.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_feedback.id) }}">
    <button class="btn">Delete</button>
</form>
