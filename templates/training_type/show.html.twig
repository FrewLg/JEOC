{% extends 'base.html.twig' %}

{% block title %}TrainingType{% endblock %}

{% block body %}
    <h1>TrainingType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('training_type_index') }}">back to list</a>

    <a href="{{ path('training_type_edit', {'id': training_type.id}) }}">edit</a>

    {{ include('training_type/_delete_form.html.twig') }}
{% endblock %}
