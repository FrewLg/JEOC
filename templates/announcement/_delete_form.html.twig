     
           <a href="{{ path('announcement_show', {'id': announcement.id}) }}" class="btn btn-soft-info">Show</a>
     <a href="{{ path('announcement_edit', {'id': announcement.id}) }}" class="btn btn-xs btn-soft-primary">Edit</a>
<form method="post" action="{{ path('announcement_delete', {'id': announcement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ announcement.id) }}">
    <button class="btn btn-xs  btn-soft-danger">Delete</button>

</form>
